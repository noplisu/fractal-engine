[project]
name = "fractal-engine"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "openai>=2.15.0",
    "micro-rag",
]

[tool.uv.sources]
micro-rag = { git = "https://github.com/noplisu/micro-rag.git" }
